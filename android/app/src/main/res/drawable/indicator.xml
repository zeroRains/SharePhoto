<?xml version="1.0" encoding="utf-8"?><!-- 若不设置 gravity 则默认是居左显示，需要设置为水平居中显示 -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:width="60dp"
        android:height="6dp"
        android:gravity="center_horizontal" >
        <shape >
            <corners android:radius="5dp" />
        </shape>

    </item>
</layer-list>